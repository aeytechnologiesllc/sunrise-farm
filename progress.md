Original prompt: Get Sunrise Farm going right now for soft launch: prioritize Itch/PWA, add crash/analytics tracking, and explore Zulura admin embedding.

Progress:
- Current branch starts clean at codex/mobile-story-perf-fixes.
- Goal is launch plumbing that is safe without external credentials: configurable telemetry endpoint, parent-window postMessage bridge for Zulura/admin iframe, Itch packaging docs/script.

TODO:
- Add telemetry bridge/client.
- Add launch docs and package script.
- Run tests/build/live mobile smoke.
- If a Zulura admin repo is identified, wire iframe tab or document exact integration.
- Added vendor-neutral telemetry module, iframe postMessage bridge, Itch packaging script, launch docs, and drop-in Zulura admin tab sample.
- Zulu-Royal local repo exists but is dirty/out-of-sync, so do not edit it in this pass without explicit confirmation or a clean worktree.
- Fixed iframe/private-mode storage crash by routing local/session storage through safe wrappers with memory fallback.
- Verified iframe postMessage bridge and local telemetry endpoint on a 390x844 mobile viewport; boot/ready/snapshot messages worked.
- Ran npm test, npm run package:itch, and the develop-web-game Playwright client; screenshots inspected.
- Emergency rollback: user reported the `500aa23` mobile perf commit made the game unplayably laggy. Reverted it immediately; subagent flagged grass chunk sizing and shadow cadence as the most suspect areas for any smaller future retry.
- Safe mobile retry: applied only a touch-device post-processing bypass in `src/main.ts`; did not touch grass chunking, shadow cadence, DPR, or world geometry. Verified against the correct Farm dev server on port 5199 with mobile smoke/profile plus npm test/build. Android emulator retry reached boot but ADB/CDP hung, so final validation used browser mobile viewport rather than unreliable emulator metrics.
- Mobile grass simplification: removed high-frequency ground blade ticks/detail shader, disabled mobile instanced grass, and verified smoother iPhone 15 Pro Max Simulator perf at `?perfRun=1&wipe=1` (60fps, 0 current 2s hitches, 28 draws / 21,772 tris in captured run).
- Validation: `npm test`, `npm run package:itch`, and `zipinfo -t sunrise-farm-itch.zip` passed after the smooth-grass patch.
- Desktop keyboard pass: joystick now hides only under explicit desktop-controls mode, keyboard help shows WASD/Space/F, Space plants/harvests from the real parcel plot when the HUD stack is stale (E remains a legacy alias), F opens fence editor, D/G/R/1/2/3 switch fence tools, and Esc closes without leaking movement.
- Touch/mobile pass: iPhone-class mobile viewport keeps joystick visible, hides key help/keycaps, and uses the touch onboarding chip.
- Real iOS Simulator performance: iPhone 15 Pro Max Safari `?perfRun=1&wipe=1` at 105.6s showed avg 16.67ms, 60fps, DPR 1.05, 32 draws / 21,898 tris, 2s window 17ms with 0/0 hitches, 0 >50ms frames.
- Real iOS Simulator performance: iPhone 17 Safari long run at 178s showed avg 16.69ms, 60fps, DPR 0.82, 29 draws / 21,772 tris, 2s window 18ms with 0/0 hitches; only 4 total >50ms frames across the whole run.
- Validation: required develop-web-game Playwright client screenshot inspected, `npm test`, `npm run package:itch`, and `zipinfo -t sunrise-farm-itch.zip` passed after the keyboard/mobile grass changes.
- Horse/tractor pass: added QA-only `perfRun` scenarios for `scenario=horse` and `scenario=tractor`, verified Hazel mounts cleanly, and blocked accidental crop auto-harvest while riding.
- Tractor gameplay pass: tractor now mounts/drives as a vehicle, can be parked/saved, plays tractor SFX while moving, harvests ready field plots on drive-over, plants wheat on empty field plots, and still supports bulk `Harvest ready plots` / `Sow ALL plots` near the tractor.
- Live mobile assertions: headed Playwright at 390x844 passed horse crop-safety and tractor harvest+empty-plant checks with no console crashes; screenshots saved under `output/horse-tractor-assertions/`.
- Real iOS Simulator QA: iPhone 15 Pro Max Safari tractor scenario showed 60fps, DPR 0.85, 69 draws / 36,116 tris, current 2s window 17ms with 0/0 hitches; only boot/setup hitches remained.
- Real iOS Simulator QA: iPhone 17 Safari horse scenario showed 60fps, DPR 1.05, 63 draws / 29,874 tris, current 2s window 18ms with 0/0 hitches; only one boot/setup >50ms frame remained.
- Validation: `npm test`, `npm run package:itch`, and `zipinfo -t sunrise-farm-itch.zip` passed after horse/tractor changes.
- Tractor row-assist pass: `Work all rows` is now the primary tractor action near the tractor, `Auto rows` is primary while driving, and the row route uses the real auto-walk controller so one button drives the tractor through field rows.
- Tractor crop pass: driving over a ready plot now harvests and replants wheat in the same pass; driving over an empty plot plants wheat without extra button presses.
- Horse polish pass: Hazel now has continuous hoof feedback while moving plus a `Trail ride` action that follows the farm road and can be interrupted by manual steering.
- Desktop controls pass: Space is now the primary activate key in HUD keycaps, helper copy, and onboarding hints; E remains as a legacy alias. Desktop still hides the virtual joystick.
- Boot gate pass: loading veil now shows asset progress, then holds through renderer warmup/stable frames before revealing gameplay; `window.__farm.boot()` exposes hidden boot metrics so boot spikes are separated from visible runtime performance.
- Live QA: headed browser assertions passed for desktop Space-to-plant, hidden desktop joystick, tractor auto-row harvest/replant, and horse trail actions; screenshots saved under `output/row-assist-qa/`.
- Real iOS Simulator QA: iPhone 15 Pro Max tractor row-assist screenshot showed 60fps, DPR 0.82, current 2s window 18ms with 0/0 hitches; boot/setup spikes were hidden behind the loading gate.
- Real iOS Simulator QA: iPhone 17 horse trail screenshot showed 60fps, DPR 1.05, current 2s window 18ms with 0/0 hitches; only one total >50ms frame remained in the hidden/early run.
- Validation: `npm test`, `npm run package:itch`, and `zipinfo -t sunrise-farm-itch.zip` passed after the row-assist/loading/horse pass.
- Tractor rider pose pass: replaced the tractor mount's standing farmer with a tractor-specific seated rider proxy, left Hazel's normal mount unchanged, and verified desktop/iPhone simulator screenshots show the farmer seated on the tractor seat.
- Tractor mobile perf pass: quieted per-plot harvest/plant effects during tractor automation, keeping batched tractor feedback so auto rows no longer spam coin/text bursts on mobile.
- UI overlap pass: changed routine `Rows done` feedback from a center ceremony banner to local float text so it does not cover existing mobile prompts.
- Validation: required develop-web-game Playwright client screenshot inspected, tractor row state assertion passed, iPhone 15 Pro Max and iPhone 17 simulator screenshots inspected, `npm test`, `npm run package:itch`, and `zipinfo -t sunrise-farm-itch.zip` passed.
- Horse rider pose pass: replaced Hazel's mounted standing farmer with a horse-specific seated rider proxy, saddle blanket/seat, forward riding arms, and shorter bent legs so the rider reads seated in the saddle instead of standing on Hazel.
- Horse pose validation: inspected required headed browser screenshot plus iPhone 15 Pro Max and iPhone 17 simulator screenshots; current simulator overlays showed 60fps with 0 current 2s hitches.
- Validation: `npm test`, `npm run package:itch`, and `zipinfo -t sunrise-farm-itch.zip` passed after the horse-rider polish.
- Tractor handling debug pass: replaced manual tractor driving with dedicated throttle/reverse/steering physics, reduced the tractor speed cap, smoothed steering, snapped tractor visual yaw to the vehicle heading, and made wheel roll/front-wheel steer telemetry visible in QA.
- Live QA URL: iPhone 17 Pro Max Simulator is open on `http://127.0.0.1:5181/?wipe=1&qa=tractor&driveDebug=1`, with a smaller top debug strip showing speed, steer, yaw, front wheel angle, row mode, and FPS.
- Tractor telemetry validation: headed Playwright drive sequence passed straight-line drift, straight yaw drift, right-turn bite, left steer, and reverse-speed assertions; screenshot saved at `output/tractor-drive-debug/drive-sequence-final.png`.
- Simulator validation: captured iPhone 17 Pro Max screenshot at `output/simulator/iphone17-pro-max-tractor-drive-debug-final.png`; tractor is mounted, seated rider reads correctly, camera follows, and HUD/dialogs remain playable.
- Validation: `npm run build`, `npm test`, `npm run package:itch`, and `zipinfo -t sunrise-farm-itch.zip` passed after the tractor handling/debug patch.
- Tractor fast-feel pass: raised tractor forward speed to slightly above player run speed, shaped joystick throttle so diagonal steering keeps power, kept reverse slower, and added tractor-only camera follow assist so mobile forward does not read sideways.
- Tractor rider pose pass: moved the rider onto the tractor's actual seat axis, raised hips onto the seat, shortened/bent legs so they no longer hang under the tractor, and kept arms forward toward the steering wheel.
- Tractor fast validation: headed mobile Playwright telemetry showed straight speed `4.14`, diagonal steering speed `3.88`, front wheel steer `0.381`, synced visual yaw, and no assertion failures; screenshot saved at `output/tractor-fast-drive-debug/tractor-fast-drive-final.png`.
- Simulator validation: iPhone 17 Pro Max Simulator is open on `http://127.0.0.1:5182/?wipe=1&qa=tractor&driveDebug=1`; screenshot saved at `output/simulator/iphone17-pro-max-tractor-fast-seat-final.png`.
- Validation: required develop-web-game client screenshot inspected, `npm test`, `npm run package:itch`, and `zipinfo -t sunrise-farm-itch.zip` passed after the fast tractor/seat/camera pass.
- Tractor car-control pass: reverse speed now matches forward speed, reverse steering keeps the normal car/backing-up sign flip, and tractor mounts inherit the parked tractor heading instead of the player's stale heading.
- Stuck-recovery pass: tractor bounds/fence/building collisions now dampen with `bumpTractor()` instead of full-stopping, and building collision skips the tractor footprint while driving so it cannot block itself.
- Control validation: headed mobile Playwright assertions passed for right-only no-move steering, forward-right, forward-left, reverse speed, reverse-right opposite yaw, reverse-left opposite yaw, and bump/reverse recovery; screenshot saved at `output/tractor-control-assertions/tractor-controls-final.png`.
- Simulator validation: iPhone 17 Pro Max Simulator is open on `http://127.0.0.1:5182/?wipe=1&qa=tractor&driveDebug=1`; screenshot saved at `output/simulator/iphone17-pro-max-tractor-controls-final.png`.
- Validation: required develop-web-game client screenshot inspected, `npm test`, `npm run package:itch`, and `zipinfo -t sunrise-farm-itch.zip` passed after the car-control/stuck-recovery pass.
- Tractor steering inversion fix: corrected forward vehicle steering so right input increases heading and left input decreases heading; row-assist steering sign was updated to match, while the tractor wheel visual adapter stays inverted for model-space alignment.
- Steering validation: headed mobile Playwright telemetry passed forward-right `+0.89`, forward-left `-0.87`, reverse-right `-0.72`, reverse-left `+0.87`, and straight reverse speed `-4.13`; screenshots saved under `output/tractor-steering-live-fixed/`.
- Mobile/emulator validation: required develop-web-game client screenshot inspected, iPhone 17 Pro Max Simulator loaded the fixed tractor QA URL, screenshot saved at `output/simulator/iphone17-pro-max-tractor-steering-fixed-wait.png`.
- Validation: `npm run build`, `npm test`, `npm run package:itch`, and `zipinfo -t sunrise-farm-itch.zip` passed after the steering sign correction.
- Itch release-candidate pass: fixed missing favicon 404 by adding `./icon.svg`, delayed the portrait rotate hint until after first-session onboarding, generated clean release media under `media/itch/`, and updated `README.md` plus `docs/LAUNCH.md` with exact Itch draft fields.
- Release E2E validation: dev and production static builds passed desktop fresh keyboard/onboarding, mobile fresh joystick/onboarding, mobile tractor right-turn + auto rows, and mobile horse trail checks with no console errors; reports saved under `output/release-e2e/` and `output/release-e2e-prod/`.
- Production package validation: `npm run build`, `npm test`, `npm run package:itch`, `zip -T sunrise-farm-itch.zip`, and `zipinfo -t sunrise-farm-itch.zip` passed; iPhone 17 Pro Max Simulator production screenshot saved at `output/simulator/iphone17-prod-release.png`.
- Itch upload handoff: use `sunrise-farm-itch.zip` as the playable HTML upload, `media/itch/cover-630x500.png` as the cover, and the four screenshots in `media/itch/` for the project page; stop before publish/submit.
- Final Itch polish pass: first-plant onboarding now points to the real east-gate field path, mobile fence editor wraps instead of clipping Done, desktop/postal default disables shadow-map GPU warnings unless `?shadows=1`, and Hazel mounts onto the open trail with a side camera so horse/rider are visible immediately.
- Final release validation: `npm run build`, `npm test`, blocker QA, dev E2E, production E2E, `npm run package:itch`, `zip -T`, `zipinfo -t`, required develop-web-game client, and iPhone 17 Pro Max Simulator Safari screenshot all passed.
- Itch-ready assets refreshed: upload `sunrise-farm-itch.zip`, cover `media/itch/cover-630x500.png`, screenshots in `media/itch/`, and simulator proof at `output/simulator/iphone17-final-itch-release-2.png`.
- Itch launch: published Sunrise Farm at `https://aey2456.itch.io/sunrise-farm`; final public checks passed on anonymous desktop and mobile browser contexts, plus Chrome preview confirmed fullscreen and keyboard controls work in the Itch embed.
- Itch CTR page polish: fixed the blank gray `Run game` frame by adding a branded `Embed BG`, added a top banner, switched screenshots to sidebar, warmed the page theme, and changed embed size to 960 × 540 to remove desktop horizontal overflow.
- Itch page validation: public desktop render has no horizontal overflow, shows banner/poster/screenshots, and public iPhone-style render has no horizontal overflow with itch's mobile-safe launcher; proof screenshots saved at `output/itch-public-ctr-desktop-final2.png` and `output/itch-public-ctr-iphone-final.png`.
- Itch Diner-style correction: replaced the busy gameplay-HUD poster/banner/sidebar layout with one clean generated farm key-art embed poster, removed the top banner, hid above-fold screenshot sidebar, restored neutral Diner-like colors, and set the embed to 640 × 360.
- Itch click validation: live desktop `Run game` click loaded the iframe at `https://html-classic.itch.zone/html/17947038/index.html?v=1781558316`; iPhone 17 Pro Max Simulator screenshot confirmed the mobile Run Game button is visible/tappable. Proof saved under `output/itch-page-polish/`.
- Mobile smoothness pass — real-device diagnosis: established that ALL prior 60fps "QA" ran on the iOS Simulator / desktop mobile-viewport (host Mac GPU), so it never reflected a real phone. Measured the actual workload — even standing in the fully-loaded east field it is only ~139 draws / 57k tris at a flat 16.7ms — proving the lag is NOT a GPU/spec ceiling (a 16/17 Pro Max would not help) but stalls + camera feel.
- Fix 1 — GPU pre-warm: `renderer.compileAsync` + a high-oblique whole-world render sweep in `boot()` before the veil lifts, so first-view shader-compile / geometry-upload no longer stalls the first run east to the field.
- Fix 2 — killed the run FOV nudge (`FollowCamera` `FOV_RUN` 45→42): the owner read the sprint lens-widen as the screen "breathing bigger"; verified FOV now flat at 42 while running.
- Fix 3 — fixed-timestep player interpolation (`Engine.alpha` + Player `logicalPos`/`prevPos`/`renderInterpolated`/`resetInterp`): the farmer mesh now glides between 60Hz steps so it stops juddering on 120Hz/ProMotion phones. Camera + horse + tractor follow the interpolated `renderPos`; gameplay/collision still read the logical pos; all teleports reset interp + a >1.5u auto-snap safety net.
- On-device perf HUD (`src/ui/perfHud.ts`): live fps / frame-time / 2s-worst / hitch counts / dpr / draw / tri, toggled by `?fps=1`, a three-finger tap, or `window.__farmFps()` — so perf reads on the real handset without a cable.
- PWA freshness: no service worker exists, so content-hashed JS busts itself; added a build-stamped `dist/version.json` (via `scripts/build.mjs` + `VITE_APP_VERSION`) and a boot-time no-store check that hard-reloads past a cached `index.html` when a newer build is live (guarded against loops). Verified the update redirect fires on a simulated newer deploy and is a clean no-op when versions match.
- Validation: `npm test` (304 pass), `npm run build`, typecheck; dev + prod-preview boot; a multi-agent site map + adversarial review of the interpolation diff returned 0 confirmed issues; on-foot / warp / tractor / horse all regression-clean on desktop. Real 120Hz-device smoothness to be confirmed by the owner via the on-screen HUD.
