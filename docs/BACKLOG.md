# Sunrise Farm — backlog (what the loop pulls from)

The improve loop works this list top-down within its allowed scope. Items are tagged:
`[bug]` (auto-fixable), `[polish]`/`[balance]` (auto with care, escalate if it changes feel),
`[feature]` (needs owner sign-off on the idea first), `[taste]` (owner-only — never auto).

Keep it ordered by value. The loop appends what it finds and checks off what it ships.

## Now (next passes)
- `[balance]` On-device economy pacing pass — needs the owner to FEEL it on the phone and
  report where it drags or where money piles up. (loop can prep proposals; owner judges)
- `[polish]` Decor art: flowerbed / planter / sapling read small + plain up close — richer
  meshes, better silhouettes.
- `[polish]` Onboarding chips: first time each new board/shop/saddle appears, a one-time
  "here's how" nudge (contracts, catalog, riding, upgrades).

## Soon
- `[feature]` **Ride into Millbrook** — the owner's wish. Today the world stops at the gate
  (WORLD_BOUNDS.maxX 22); the town is look-only. Make it rideable/walkable past the gate,
  buildings solid, dismount-to-enter the café. (BIG — confirm approach with owner first.)
- `[feature]` Decor variety + themed sets (fountain, lanterns, seasonal) — deepen the
  infinite cosmetic sink beyond 10 items.
- `[polish]` Action buttons (bottom-right verbs) still bright cream — soften toward the new
  frosted HUD while staying clearly tappable. (owner leaning: subtle — confirm)

## Later
- `[feature]` Town acts 8+ / festivals as events.
- `[feature]` Deed-ceremony bespoke theater polish (SOLD repaint flourish).
- `[polish]` Collect-combo riser, toast queue discipline, cine-shot whiskers.

## Won't-do unless asked (judged not worth churn)
- Remove the no-subscriber events (chipDone/townBuilt/fenceStyleBought) — harmless; their
  side-effects run inline. Cleanup-only, zero benefit.

## Taste questions parked for the owner
- Wool *contracts* now get the Works 1.5x (shipped) — keep, or contracts on base rate?
- Milestone tone past lvl 50 (currently generic) — add more, or let it rest?
